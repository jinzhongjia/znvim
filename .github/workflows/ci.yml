# http://webui.me
# https://github.com/webui-dev/zig-webui
# Licensed under MIT License.
# All rights reserved.

name: CI
on:
  push:
    paths-ignore: ['**/*.md']
  pull_request:
    branches: [main]
    paths-ignore: ['**/*.md']

jobs:
  linux:
    uses: ./.github/workflows/linux.yml
    permissions:
      contents: write